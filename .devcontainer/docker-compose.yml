services:
  rust:
    build:
      dockerfile: ./Dockerfile
    volumes:
      - ../:/app
    working_dir: /app
    environment:
      - USER=rust
    command: /bin/sh -c "while sleep 1000; do :; done"